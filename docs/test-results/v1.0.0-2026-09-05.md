# SafetyFirstEMA v1.0.0 — Verification Record

Date: 2026-09-05 UTC

## Environment

- Ubuntu 24.04 x86_64
- Wine Staging 11.16
- MetaTrader 5 x64 build 6180
- MetaEditor build 6180
- Live trading was not enabled and no broker order was submitted

## Static checks

Command:

```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
```

Result: PASS, 11/11 tests.

The checks cover the disabled live-trading default, closed-bar indicator shifts,
risk controls, hedging guard, broker-side stop prices, persistent risk locks,
explicit tester parameters, and deterministic/random-latency tester configs.

## Compile

MetaEditor compiled the repository source with the MT5 standard include tree.
The first compile found warning 68 because a major-zero version is not accepted
by the current MQL Market format. The EA metadata was changed to `1.000` and the
semantic project version to v1.0.0, then compiled again.

Final result:

```text
Result: 0 errors, 0 warnings, 773 ms elapsed, cpu='X64 Regular'
```

Compiled artifact: `SafetyFirstEMA.ex5` (51,752 bytes).

## MetaTrader 5 and Strategy Tester

The actual terminal launched successfully and logged:

```text
MetaTrader 5 x64 build 6180 started for MetaQuotes Ltd.
Windows 11 build 22000 on Wine 11.16 Linux
```

The headless tester loaded `tests/mt5/strategy_tester.ini`, but stopped before
loading the EA:

```text
tester not started because the account is not specified
tester didn't start
shutdown with -1000012353 (tester not started because the account is not specified)
```

Adding a nonzero tester-only login and an offline common login did not create an
authorized terminal account and produced the same result. The ineffective login
workaround was removed. No credentials are stored in the repository.

ดังนั้น **ยังไม่ได้ทดสอบ EA ใน MetaTrader 5 จริง**: the terminal itself was
opened, but Strategy Tester did not reach `OnInit`, indicator creation, price
data processing, or trade simulation.

## Logs reviewed

- Terminal Journal: terminal startup succeeded; tester account error above
- Strategy Tester log: cloud agents disabled; tester account error above
- Experts log: not created because the EA was never initialized
- No EA runtime, indicator, volume, price, stop, order, or position result can
  be inferred from this failed start

## Backtest metrics

| Metric | Result |
|---|---:|
| Net profit | N/A |
| Profit factor | N/A |
| Maximum drawdown | N/A |
| Recovery factor | N/A |
| Win rate | N/A |
| Trades | 0 (tester did not start) |
| Average trade | N/A |
| Expected payoff | N/A |
| Sharpe ratio | N/A |
| Long/short | 0/0 |
| Wins/losses | 0/0 |
| Consecutive wins/losses | N/A |

## Required next step

Provision an authorized MT5 hedging demo account in the test terminal, then run
both checked-in configs. Only after those runs should runtime defects,
performance, out-of-sample behavior, or strategy changes be evaluated.
